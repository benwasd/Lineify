machine:
    environment:

dependencies:
    override:
        - chmod +x gradlew
        - ./gradlew dependencies
    pre:
        - echo y | android update sdk --no-ui --all --filter tools,build-tools-24.0.0-preview,extra-google-m2repository,extra-google-google_play_services,extra-android-support
